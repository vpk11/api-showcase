<% present(@api) do |api| %>
  <%= react_component(
    'ShowApi',
    {
      api: api,
      version: api.version,
      params: api.params,
      headers: api.headers,
      bodies: api.bodies,
      responses: api.responses,
    })
  %>
<% end %>
